# Built mint artifact — regenerate with `python3 build-standalone.py` (inlines Three.js).
# Kept out of git: it's a 2.4 MB generated file that changes whenever index.html does.
index.standalone.html
# Marketplace-embed test matrix (dev tooling) — regenerate with `python3 build-embed-matrix.py`.
# ~23 MB of generated standalone copies; never deploy or commit.
embed-matrix/
